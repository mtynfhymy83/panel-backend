-- Pardis schema (reference). Actual DDL is applied by scripts/migrate.php per driver.

-- users, user_roles, course_classes, class_memberships
-- terms, teacher_grades, exams, student_messages, student_message_teachers, teacher_feedbacks
