# ---- App ----
APP_HOST=0.0.0.0
APP_PORT=9501
APP_DEBUG=1

# ---- Swoole ----
SWOOLE_WORKER_NUM=4
SWOOLE_ENABLE_CURL_HOOK=false
SWOOLE_HTTP_COMPRESSION=true

# Cache instances of controllers per worker (true = faster, false = fresh each request)
CONTROLLER_CACHE=true

# ---- Auth (JWT) ----
JWT_SECRET=REPLACE_WITH_AT_LEAST_32_RANDOM_BYTES
JWT_ALGO=HS256
JWT_TTL=60

# ---- Cache (file | redis) ----
CACHE_DRIVER=file
REDIS_HOST=127.0.0.1
REDIS_PORT=6379
REDIS_USERNAME=
REDIS_PASSWORD=
REDIS_DB=0

# ---- Default admin (run: php scripts/seed.php) ----
ADMIN_FIRST_NAME=System
ADMIN_LAST_NAME=Admin
ADMIN_PASSWORD=change-me-in-production
ADMIN_PHONE=09120000001

# ---- Database ----
# Default: SQLite (zero setup — runs out of the box). Run: php scripts/migrate.php
DB_DSN=sqlite:storage/app.db
DB_USERNAME=
DB_PASSWORD=
DB_POOL_SIZE=10

# PostgreSQL:
# DB_DSN=pgsql:host=127.0.0.1;port=5432;dbname=app
# DB_USERNAME=postgres
# DB_PASSWORD=secret

# MySQL:
# DB_DSN=mysql:host=127.0.0.1;port=3306;dbname=app;charset=utf8mb4
# DB_USERNAME=root
# DB_PASSWORD=secret

# Pool tuning (optional)
# DB_MAX_IDLE_TIME=600
# DB_POOL_POP_TIMEOUT_SECONDS=1.0
# DB_POOL_LEAK_DETECTION_ENABLED=1
# DB_POOL_LEAK_THRESHOLD_MS=15000

# ---- SMS (fake | smsir) ----
SMS_DRIVER=fake
SMS_IR_API_KEY=
SMS_IR_TEMPLATE_ID=
SMS_IR_OTP_PARAMETER=CODE
